import { InjectHTML } from './inject-html';

describe('InjectHTML', () => {
  it('should create an instance', () => {
    const directive = new InjectHTML();
    expect(directive).toBeTruthy();
  });
});
